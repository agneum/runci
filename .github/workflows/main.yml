on: [ push ]

jobs:
  migration_job:
    runs-on: ubuntu-latest
    name: CI migration
    steps:
#      - name: Checkout
#        uses: actions/checkout@v2
#      - name: Check owner
#        id: owner
#        uses: ./migration-action
#        with:
#          owner: 'agneum'
#          repo: 'runci'
#          ref: 'master'
#          commands:  |
#            sqitch deploy
#            echo 'Migration has been completed'

      # Use public actions
      - name: DB migration with DLE
        uses: agneum/migration-action@v0.0.1
        id: db-migration
        with:
          owner: 'agneum'
          repo: 'runci'
          ref: 'master'
          commands:  |
            sqitch deploy
            echo 'Migration has been completed'
        env:
          CI_ENDPOINT: ${{ secrets.CI_ENDPOINT }}
          SECRET_TOKEN: ${{ secrets.SECRET_TOKEN }}
      - name: Get the output status
        run: echo "Status ${{ steps.db-migration.outputs.status }}"
